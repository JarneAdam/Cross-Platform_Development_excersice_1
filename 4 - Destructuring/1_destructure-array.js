/**
 * Gebruik destructuring om het eerste en het derde element van de array op te halen.
 */
const values = [1, 2, 3, 4];

// wijzig de volgende twee lijnen
const first = values[0];
const third = values[2];

console.log(first, third);